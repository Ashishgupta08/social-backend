const User = require('./user.route');
const Connection = require('./connection.route');
const Post = require('./post.router');
const GetFeed = require('./getFeed.route');

module.exports = { User, Connection, Post, GetFeed }